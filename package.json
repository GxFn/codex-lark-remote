{
  "name": "codex-lark-remote-repo",
  "version": "0.0.0",
  "private": true,
  "description": "Development workspace for the Lark Remote plugin.",
  "type": "module",
  "workspaces": [
    "plugins/codex-lark-remote"
  ],
  "scripts": {
    "test": "node --test",
    "fixture": "node ./scripts/lark-event-fixture.mjs"
  },
  "overrides": {
    "axios": "^1.16.0"
  },
  "engines": {
    "node": ">=20"
  }
}
